{
  "$schema": "https://www.schemastore.org/package.json",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "start": "php -S localhost:8000 -t public",
    "predeploy": "npm run build",
    "deploy": "php artisan down && git push railway main && php artisan up"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "concurrently": "^9.0.1",
    "laravel-vite-plugin": "^3.1",
    "tailwindcss": "^4.0.0",
    "vite": "^8.0.0"
  },
  "dependencies": {
    "alpinejs": "^3.15.12"
  },
  "engines": {
    "node": ">=18"
  }
}
